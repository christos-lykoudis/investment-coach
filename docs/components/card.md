# Card

Location: `apps/web/components/ui/Card.tsx`

Simple bordered container used throughout the MVP screens.

## Props

- `title?` (`string`): Optional title rendered at the top.
- `children` (`React.ReactNode`): Content inside the card.

## Notes

- Uses neutral borders and spacing via CSS variables defined in `apps/web/app/globals.css`.

